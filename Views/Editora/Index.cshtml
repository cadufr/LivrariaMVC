@model IEnumerable<Livraria.Models.Editora>
@{
    ViewData["Title"] = "Editoras";
}
<h1>@ViewData["Title"]</h1>

@if(Model.Count() == 0)
{
    <p>Nenhuma Editora encontrado</p>
    <a class="btn btn-outline-dark" asp-controller="Editora" asp-action="Cadastro">Cadastre uma nova Editora</a>
}
else
{
    <table class="table table-white">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nome</th>
                <th>Descrição</th>
            </tr>
        </thead>
        <tbody>
            @foreach(var editora in Model)
            {
                <tr>
                    <td>@editora.Id</td>
                    <td>@editora.Nome</td>
                    <td>@editora.Descricao</td>
                    <td>
                        <a class="btn btn-outline-info" asp-controller="Editora" asp-action="Show" asp-route-id="@editora.Id">Ver</a>
                        <a class="btn btn-outline-warning" asp-controller="Editora" asp-action="Update" asp-route-id="@editora.Id">Update</a>
                        <a class="btn btn-outline-danger" asp-controller="Editora" asp-action="Delete" asp-route-id="@editora.Id">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
    <a class="btn btn-outline-dark" asp-controller="Editora" asp-action="Cadastro">Cadastre uma nova Editora</a>
}